import { useParams } from "react-router-dom";
import { getId, getNewId } from "../../../customHooks/getLocalstrage";

export const Edit = (props) => {
	const { id } = useParams();



	return <div>Edit</div>;
};
